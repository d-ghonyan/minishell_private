#include "minishell.h"

void	expand(t_cmd *cmd)
{
	int	i;

	i = 0;
	while (i < cmd->len)
	{
		
	}
}

char	*expand_line(char *cmd)
{
	int		i;
	char	*s;

	i = 0;
	// s = malloc(sizeof (*s) * expanded_len(cmd));
	if (!s)
		return (NULL);
	while (s[i])
	{
		i++;
	}
}